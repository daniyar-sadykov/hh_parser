# 🚀 ОБНОВЛЕНИЕ API: Полный поиск + Умное ограничение

## ✨ ЧТО ИЗМЕНИЛОСЬ

Теперь API работает по новой логике:

1. **Backend ищет ВСЕ вакансии** (до 10 000 штук)
2. **Сортирует по дате публикации** (самые свежие первыми)
3. **Возвращает только N самых свежих** (параметр `limit`)

---

## 🎯 ЗАЧЕМ ЭТО НУЖНО

**Проблема раньше:**
- Ограничение в 2000 вакансий могло пропустить свежие вакансии
- Не всегда возвращались самые актуальные

**Решение сейчас:**
- Находим **ВСЕ** подходящие вакансии
- Возвращаем только **N самых свежих**
- N8N получает готовый результат без дополнительной фильтрации

---

## 📝 НОВЫЕ ПАРАМЕТРЫ

### **Параметр `limit`** (новый!)

```json
{
  "limit": 20  // Сколько ВЕРНУТЬ самых свежих вакансий
}
```

- **По умолчанию:** 20
- **Диапазон:** от 1 до 1000
- **Что делает:** Ограничивает количество вакансий в ответе

### **Параметр `max_results`** (изменён)

```json
{
  "max_results": 10000  // Сколько ИСКАТЬ вакансий (внутренний параметр)
}
```

- **По умолчанию:** 10000 (ищет максимум!)
- **Диапазон:** от 1 до 10000
- **Что делает:** Определяет, сколько страниц парсить

---

## 🔌 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### **1. Основной endpoint: `/api/search`**

#### **Запрос:**

```bash
POST https://your-api.com/api/search
Content-Type: application/json

{
  "keywords": "оператор CRM входящие заявки",
  "region": 1,
  "min_salary": 50000,
  "only_with_salary": true,
  "period": 7,
  "limit": 20,
  "max_results": 10000
}
```

#### **Ответ:**

```json
{
  "success": true,
  "count": 20,
  "message": "Найдено 1247 вакансий, возвращено 20 самых свежих",
  "statistics": {
    "total_found": 1247,
    "returned_count": 20,
    "with_salary": 18,
    "with_salary_percent": 90.0,
    "unique_companies": 19,
    "search_params": {
      "keywords": "оператор CRM входящие заявки",
      "region": 1,
      "min_salary": 50000,
      "period_days": 7,
      "limit": 20
    }
  },
  "vacancies": [
    {
      "id": "127525558",
      "название": "Менеджер по работе с клиентами",
      "компания": "99 БАЛЛОВ",
      "оплата": "50 000 - 80 000 руб.",
      "дата_публикации": "2025-12-10T18:30:00+0300",
      "ссылка": "https://hh.ru/vacancy/127525558",
      ...
    },
    // ... ещё 19 самых свежих
  ]
}
```

---

### **2. Быстрый endpoint: `/api/search-quick`**

#### **Запрос:**

```bash
POST https://your-api.com/api/search-quick?keywords=оператор+CRM&region=1&limit=50
```

или

```bash
POST https://your-api.com/api/search-quick
Content-Type: application/json

{
  "keywords": "оператор CRM",
  "region": 1,
  "limit": 50
}
```

#### **Ответ:**

```json
{
  "success": true,
  "total_found": 1247,
  "returned_count": 50,
  "vacancies": [...]
}
```

---

## 🔧 ИНТЕГРАЦИЯ С N8N

### **Вариант 1: HTTP Request Node**

```javascript
// HTTP Request Node настройки:
Method: POST
URL: https://your-api.com/api/search
Body (JSON):
{
  "keywords": "{{$json.keywords}}",
  "region": {{$json.region}},
  "limit": {{$json.limit || 20}},  // Из входящего запроса
  "min_salary": 50000,
  "only_with_salary": true,
  "period": 7
}
```

### **Вариант 2: Code Node (если нужна обработка)**

```javascript
// Code Node
const keywords = $input.item.json.keywords;
const limit = $input.item.json.limit || 20;

const response = await $http.request({
  method: 'POST',
  url: 'https://your-api.com/api/search',
  body: {
    keywords: keywords,
    region: 1,
    limit: limit,
    min_salary: 50000,
    only_with_salary: true,
    period: 7
  }
});

return {
  json: {
    success: response.data.success,
    total_found: response.data.statistics.total_found,
    returned: response.data.count,
    vacancies: response.data.vacancies
  }
};
```

---

## 📊 ЛОГИКА РАБОТЫ

```
┌─────────────────────────────────────────┐
│ 1. N8N отправляет запрос:               │
│    - keywords: "оператор CRM"            │
│    - limit: 20                          │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 2. Backend (hh_parser.py):              │
│    - Парсит ВСЕ страницы (до 100)       │
│    - order_by = 'publication_time'      │
│    - Находит: 1247 вакансий             │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 3. Backend (api.py):                    │
│    - Сортирует по дате (свежие первыми) │
│    - Берёт только первые 20             │
│    - all_vacancies[:limit]              │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 4. N8N получает:                        │
│    {                                    │
│      "total_found": 1247,               │
│      "returned_count": 20,              │
│      "vacancies": [20 самых свежих]     │
│    }                                    │
└─────────────────────────────────────────┘
```

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### **В `hh_parser.py`:**

```python
def search_vacancies(
    ...
    max_pages: Optional[int] = 100,  # По умолчанию до 100 страниц
    order_by: str = 'publication_time'  # ВСЕГДА по дате!
):
    # Парсим ВСЕ страницы
    while page < max_pages:
        # ... парсинг ...
    
    return all_vacancies  # Возвращаем ВСЕ
```

### **В `api.py`:**

```python
# 1. Ищем ВСЕ
all_vacancies = parser.search_vacancies(
    order_by='publication_time',
    max_pages=100
)

# 2. Сортируем (на всякий случай)
all_vacancies.sort(
    key=lambda x: x.get('дата_публикации', ''), 
    reverse=True  # Новые первыми
)

# 3. Ограничиваем до limit
freshest_vacancies = all_vacancies[:request.limit]

# 4. Возвращаем
return {
    "total_found": len(all_vacancies),
    "returned_count": len(freshest_vacancies),
    "vacancies": freshest_vacancies
}
```

---

## ✅ ПРЕИМУЩЕСТВА

### **1. Полнота поиска:**
- ✅ Находим **ВСЕ** подходящие вакансии
- ✅ Не пропускаем свежие из-за ограничений

### **2. Актуальность:**
- ✅ Всегда возвращаем **самые свежие**
- ✅ Приоритет недавно опубликованным

### **3. Экономия трафика:**
- ✅ N8N получает только нужное количество
- ✅ Меньше данных для передачи

### **4. Гибкость:**
- ✅ Можно менять `limit` в каждом запросе
- ✅ Одна и та же логика для всех случаев

---

## 🎯 РЕКОМЕНДУЕМЫЕ НАСТРОЙКИ

### **Для автооткликов:**
```json
{
  "keywords": "оператор CRM входящие",
  "region": 1,
  "min_salary": 50000,
  "only_with_salary": true,
  "period": 7,
  "limit": 20,          // 20 самых свежих в день
  "max_results": 10000  // Искать максимум
}
```

### **Для аналитики:**
```json
{
  "keywords": "оператор CRM",
  "region": 113,  // Вся Россия
  "limit": 100,   // Больше для анализа
  "period": 30    // За месяц
}
```

### **Для срочного поиска:**
```json
{
  "keywords": "оператор CRM",
  "period": 1,    // За последний день
  "limit": 10     // Топ-10 самых свежих
}
```

---

## 🔍 ПРОВЕРКА РАБОТЫ

### **Тест 1: Базовый запрос**

```bash
curl -X POST "https://your-api.com/api/search" \
  -H "Content-Type: application/json" \
  -d '{
    "keywords": "оператор CRM",
    "region": 1,
    "limit": 5
  }'
```

**Ожидаемый результат:**
- `total_found`: много (например, 1247)
- `returned_count`: 5
- `vacancies`: массив из 5 самых свежих

---

### **Тест 2: Проверка свежести**

```bash
curl -X POST "https://your-api.com/api/search" \
  -H "Content-Type: application/json" \
  -d '{
    "keywords": "оператор",
    "period": 1,
    "limit": 3
  }'
```

**Проверка:**
- Все 3 вакансии должны быть опубликованы сегодня
- Первая вакансия - самая свежая (позднее время)

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:

1. Проверьте параметр `limit` (от 1 до 1000)
2. Убедитесь, что `order_by = 'publication_time'`
3. Посмотрите `total_found` vs `returned_count` в ответе

---

## 📅 ИСТОРИЯ ИЗМЕНЕНИЙ

**Версия 2.1.0** (2025-12-10)
- ✅ Добавлен параметр `limit`
- ✅ Backend ищет ВСЕ вакансии (до 10000)
- ✅ Возвращаются только N самых свежих
- ✅ По умолчанию `order_by = 'publication_time'`

---

## 🎉 ГОТОВО!

Теперь ваш API:
- 🔍 Находит **ВСЕ** вакансии
- 📅 Возвращает **самые свежие**
- 🎯 N8N получает только **нужное количество**

**Всё работает автоматически! Просто отправляйте запросы с параметром `limit`.**

